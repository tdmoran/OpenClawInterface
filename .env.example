# OpenClaw Gateway Configuration
# WebSocket URL the browser uses to connect to the gateway
NEXT_PUBLIC_GATEWAY_URL=ws://localhost:18789
# Auth token for the gateway connection
NEXT_PUBLIC_GATEWAY_TOKEN=your-token-here

# Server-side proxy URL for the /gateway-ws rewrite (local dev only).
# On Vercel this is unused — the client connects directly via NEXT_PUBLIC_GATEWAY_URL.
# GATEWAY_PROXY_URL=http://localhost:18789

# Code Monitor Remote Access (for Vercel → local tunnel)
# Set to your cloudflared tunnel URL so the Vercel-deployed dashboard
# connects to code monitor APIs on your local dev server.
# NEXT_PUBLIC_CODE_MONITOR_URL=https://xxx.trycloudflare.com
# Set to your Vercel app URL so the local server allows cross-origin requests.
# CODE_MONITOR_CORS_ORIGIN=https://your-app.vercel.app
